export interface OnboardingStep {
    text: string
    image?: string
}

export enum ModalPositions {
    Top = 'Top',
    Bottom = 'Bottom',
    Left = 'Left',
    Right = 'Right',
    Center = 'Center'
}

export interface OnboardingStep {
    text: string
    image?: string
}
